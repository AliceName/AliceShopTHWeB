@model AliceShop.Models.Material

@{
    ViewData["Title"] = "Thêm chất liệu mới | AliceStore";
}

<main class="pt-12 pb-20 max-w-[1440px] mx-auto px-6 md:px-20 animate-fadeIn font-sans">

    <!-- KHU VỰC TIÊU ĐỀ TIÊU CHUẨN SANS-SERIF -->
    <header class="mb-12">
        <span class="text-xs font-bold text-primary Corporate uppercase tracking-widest block mb-1 font-sans">Chế độ quản trị viên</span>
        <h1 class="font-sans text-2xl md:text-3xl text-on-surface mb-2 font-bold uppercase tracking-wide">
            Khởi tạo chất liệu
        </h1>
        <p class="text-on-surface-variant/80 font-sans text-sm font-medium">
            Thêm mới các loại nguyên liệu, chất liệu quý hiếm phục vụ cho quy trình chế tác tác phẩm trang sức.
        </p>
    </header>

    <!-- KHUNG FORM NHẬP LIỆU TOÀN BỘ THEO FONT-SANS -->
    <div class="max-w-2xl bg-white p-8 md:p-10 rounded-xl border border-outline-variant/30 shadow-[0_4px_20px_rgba(0,0,0,0.01)]">
        <form asp-action="Create" method="post" class="space-y-8">

            <!-- Hộp lỗi đồng bộ font-sans -->
            <div asp-validation-summary="ModelOnly" class="text-xs font-semibold text-error bg-error-container/20 border border-error/20 p-4 rounded-md animate-fadeIn font-sans hidden" id="materialValidationSummary" role="alert"></div>

            <div class="space-y-2 group">
                <label asp-for="Name" class="block text-[10px] uppercase tracking-[0.15em] text-outline font-bold group-focus-within:text-primary transition-colors font-sans">
                    Tên chất liệu / Nguyên liệu chế tác <span class="text-error">*</span>
                </label>

                <!-- Ô input chữ Manrope đậm nét, chống vỡ dấu tiếng Việt -->
                <input asp-for="Name" autocomplete="new-password" class="w-full bg-transparent border border-outline-variant focus:border-primary rounded-lg py-3 px-4 font-sans text-sm font-semibold text-on-background focus:ring-0 outline-none transition-colors placeholder:text-outline-variant/60"
                       placeholder="Ví dụ: Vàng Hồng 14K, Bạch Kim..." required />
                <span asp-validation-for="Name" class="text-xs font-bold text-error mt-1 block font-sans"></span>
            </div>

            <!-- THANH HÀNH ĐỘNG ĐỒNG BỘ FONT NÚT BẤM -->
            <div class="pt-4 border-t border-outline-variant/20 flex flex-col sm:flex-row gap-4 items-center">
                <button type="submit" class="w-full sm:w-auto px-10 py-3.5 bg-primary text-on-primary font-sans text-xs uppercase tracking-widest rounded-lg transition-all duration-300 active:scale-[0.98] hover:bg-opacity-90 border-none outline-none shadow-md shadow-primary/10 font-bold cursor-pointer">
                    Khởi tạo chất liệu
                </button>
                <a asp-action="Index" class="w-full sm:w-auto px-10 py-3.5 border border-outline-variant text-on-surface font-sans text-xs uppercase tracking-widest rounded-lg hover:bg-surface-container-low text-center no-underline transition-all font-bold flex items-center justify-center bg-transparent box-border">
                    Hủy bỏ & Quay lại
                </a>
            </div>
        </form>
    </div>
</main>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const valSummary = document.getElementById('materialValidationSummary');
            if (valSummary) {
                const hasErrors = valSummary.querySelectorAll('li').length > 0;
                if (hasErrors) {
                    valSummary.classList.remove('hidden');
                } else {
                    valSummary.classList.add('hidden');
                }
            }
        });
    </script>
}