@model List<AliceShop.Models.UserAddress>

@{
    ViewData["Title"] = "Sổ Địa Chỉ | AliceStore";
}

<main class="flex-grow pt-12 pb-20 px-6 md:px-12 max-w-6xl mx-auto w-full animate-fadeIn font-sans">
    <div class="flex flex-col md:flex-row gap-12 items-start">

        <aside class="w-full md:w-60 flex-shrink-0 bg-white border border-outline-variant/20 rounded-xl p-6 shadow-[0_4px_20px_rgba(0,0,0,0.01)]">
            <div class="mb-8">
                <h1 class="font-serif text-2xl text-on-surface mb-1 font-medium tracking-wide">Tài khoản</h1>
                <p class="text-on-surface-variant font-sans text-xs italic font-medium truncate">Chào mừng, @(User.Identity?.Name)</p>
            </div>
            <nav class="flex flex-col space-y-3">
                <a class="flex items-center space-x-3 py-2.5 px-3 rounded-lg transition-all duration-300 text-on-surface-variant hover:text-primary hover:bg-surface-container-low/50 no-underline font-semibold text-sm" asp-area="Identity" asp-page="/Account/Manage/Index">
                    <span class="material-symbols-outlined text-xl">person</span>
                    <span class="font-sans">Thông tin cá nhân</span>
                </a>
                <a class="flex items-center space-x-3 py-2.5 px-3 rounded-lg transition-all duration-300 text-on-surface-variant hover:text-primary hover:bg-surface-container-low/50 no-underline font-semibold text-sm" asp-area="Identity" asp-page="/Account/Manage/ChangePassword">
                    <span class="material-symbols-outlined text-xl">security</span>
                    <span class="font-sans">Đổi mật khẩu</span>
                </a>

                <a class="flex items-center space-x-3 py-2.5 px-3 rounded-lg transition-all duration-300 text-on-surface-variant hover:text-primary hover:bg-surface-container-low/50 no-underline font-semibold text-sm" asp-area="" asp-controller="Profile" asp-action="Orders">
                    <span class="material-symbols-outlined text-xl">receipt_long</span>
                    <span class="font-sans">Lịch sử đơn hàng</span>
                </a>

                <a class="flex items-center space-x-3 py-2.5 px-3 rounded-lg transition-all duration-300 bg-surface-container-low text-primary no-underline font-semibold text-sm" asp-area="" asp-controller="Profile" asp-action="Index">
                    <span class="material-symbols-outlined text-xl" style="font-variation-settings: 'FILL' 1;">location_on</span>
                    <span class="font-sans">Sổ địa chỉ</span>
                </a>

                <div class="pt-4 border-t border-outline-variant/30 mt-2">
                    <form class="form-inline inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post">
                        <button type="submit" class="w-full flex items-center space-x-3 py-2.5 px-3 text-left text-error hover:bg-red-50/50 rounded-lg transition-all bg-transparent border-none cursor-pointer outline-none font-semibold text-sm">
                            <span class="material-symbols-outlined text-xl">logout</span>
                            <span class="font-sans">Đăng xuất</span>
                        </button>
                    </form>
                </div>
            </nav>
        </aside>

        <div class="flex-grow bg-white border border-outline-variant/20 rounded-xl p-8 md:p-10 shadow-[0_4px_20px_rgba(0,0,0,0.01)] min-h-[500px]">
            <header class="mb-10 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
                <div>
                    <h1 class="font-serif text-[32px] md:text-[40px] leading-tight font-medium text-on-surface mb-3 m-0">Sổ Địa Chỉ</h1>
                    <p class="font-sans text-sm md:text-base text-[#5f5e5e] max-w-xl m-0">Quản lý các địa chỉ giao hàng của bạn.</p>
                </div>
                <a asp-action="CreateAddress" class="whitespace-nowrap bg-[#2f3130] text-[#faf9f7] py-3 px-8 font-sans text-xs font-bold uppercase tracking-widest rounded-sm hover:bg-[#1a1c1b] no-underline text-center active:scale-[0.98] transition-all">
                    + Thêm địa chỉ
                </a>
            </header>

            @if (TempData["success"] != null)
            {
                <div class="text-xs text-green-700 font-semibold bg-green-50 p-4 border border-green-200 rounded mb-8">
                    @TempData["success"]
                </div>
            }
            @if (TempData["error"] != null)
            {
                <div class="text-xs text-red-600 font-semibold bg-red-50 p-4 border border-red-200 rounded mb-8">
                    @TempData["error"]
                </div>
            }

            @if (Model == null || !Model.Any())
            {
                <div class="text-center py-20 text-[#5f5e5e]">
                    <span class="material-symbols-outlined text-5xl mb-4 block">location_off</span>
                    <p class="font-sans text-base m-0">Bạn chưa có địa chỉ giao hàng nào.</p>
                </div>
            }
            else
            {
                <div class="space-y-6">
                    @foreach (var address in Model)
                    {
                        <div class="border border-[#d1c5b4]/40 rounded-lg p-6 flex flex-col sm:flex-row sm:items-start sm:justify-between gap-4 @(address.IsDefault ? "ring-1 ring-[#775a19]" : "")">
                            <div class="flex-grow">
                                <div class="flex items-center gap-3 mb-2">
                                    <span class="font-sans font-semibold text-base text-on-surface">@address.FullName</span>
                                    @if (address.IsDefault)
                                    {
                                        <span class="text-[10px] font-bold uppercase tracking-widest bg-[#775a19] text-white px-2 py-1 rounded-sm">Mặc định</span>
                                    }
                                </div>
                                <p class="font-sans text-sm text-[#5f5e5e] m-0 mb-1">@address.PhoneNumber</p>
                                <p class="font-sans text-sm text-[#5f5e5e] m-0">@address.DetailedAddress</p>
                            </div>

                            <div class="flex flex-row sm:flex-col gap-2 flex-shrink-0">
                                @if (!address.IsDefault)
                                {
                                    <form asp-action="SetDefaultAddress" method="post" class="m-0">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@address.Id" />
                                        <button type="submit" class="w-full bg-transparent border border-[#7f7667] text-on-surface py-2 px-4 font-sans text-xs font-bold uppercase tracking-widest rounded-sm hover:bg-[#e3e2e0]/20 cursor-pointer transition-all">
                                            Đặt mặc định
                                        </button>
                                    </form>
                                }

                                <form asp-action="DeleteAddress" method="post" class="m-0" onsubmit="return confirm('Bạn có chắc muốn xóa địa chỉ này?');">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@address.Id" />
                                    <button type="submit" class="w-full bg-transparent border border-red-300 text-red-600 py-2 px-4 font-sans text-xs font-bold uppercase tracking-widest rounded-sm hover:bg-red-50 cursor-pointer transition-all">
                                        Xóa
                                    </button>
                                </form>
                            </div>
                        </div>
                    }
                </div>
            }
        </div>
    </div>
</main>
